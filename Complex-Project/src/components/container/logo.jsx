import React from "react";

function Logo({width = '100px'})// If the width prop is not passed when the Logo component is used, the value of width will default to '100px' so it is an optional value
{
    return(
        <div>Logo</div>
    )
}

export default Logo